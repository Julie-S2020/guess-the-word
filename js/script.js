//Create Global Variables

 guessedLetters = document.querySelector(".guessed-letters");
const button = document.querySelector(".guess");
const textInput = document.querySelector(".letter");
const inProgress = document.querySelector(".word-in-progress");
const remainingGuesses = document.querySelector(".remaining");
const remainingGuessesDisplay = document.querySelector(".remaining"); //span?
const messages = document.querySelector(".message");
const hiddenButton = document.querySelector(".guess");
const word = "magnolia"; 

//Write a Function to Add Placeholders for Each Letter

const wordsInProgress = function () {
    if (guessedLetters 
    inProgress.innerText = "●"
}

wordsInProgress(word);